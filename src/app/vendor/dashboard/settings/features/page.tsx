import Featurespage from '@/src/components/dashboard/settings/features/components-features';
import { Metadata } from 'next';
import React from 'react';

export const metadata: Metadata = {
    title: 'Support',
};

const Features = () => {
    return <Featurespage />;
};

export default Features;
